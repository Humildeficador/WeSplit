version: '3.8'

services:
  api:
    container_name: wesplit-api
    build:
      context: .
      dockerfile: Dockerfile
    image: wesplit-backend:latest
    ports:
      - "3333:3333"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development